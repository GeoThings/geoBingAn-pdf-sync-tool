# Credentials and Secrets
credentials.json

# State files (may contain sensitive info)
state/*.json
state/*.lock
state/*.tmp.*
state/.sync_start_time
state/*.bak.*
state/*.pdf

# But keep upload history (needs to persist across clones)
!state/upload_history_all.json
!state/permit_registry.json

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
*.egg-info/
dist/
build/

# Logs
*.log
logs/*.log
logs/weekly_sync_*

# IDE
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Environment
.env
.env.*
!.env.example
venv/
env/
ENV/

# Local test artifacts
temp_pdfs/
test_io.txt

# 保留 config.py.example 作為範本
# config.py 現在從 .env 載入，不再包含敏感資訊
