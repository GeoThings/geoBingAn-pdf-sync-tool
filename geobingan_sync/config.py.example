"""
⚠️ 此檔案已棄用 - 請改用 .env.example

自 v2.3.0 起，設定已改用環境變數管理。
請參考 .env.example 建立 .env 檔案。

設定步驟：
1. cp .env.example .env
2. 編輯 .env 填入認證資訊
3. config.py 會自動從 .env 載入設定

詳見 README.md 的「設定環境變數」章節。
"""

# 此檔案保留供參考，實際設定請使用 .env
raise DeprecationWarning("請使用 .env 設定環境變數，參考 .env.example")
